import React, { ReactElement } from "react";

export type RowBodyProps = {
}

const RowBody: React.FC<RowBodyProps>
  = ({ }) => {
    return (
      <div className=" row g-3 py-1 py-xl-3">
      </div>
    );
  };

export default RowBody;